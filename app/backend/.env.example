# ============================================================
# 大模型接入模板（def 45）—— 复制本文件改名为 .env（同目录），填好后即生效
# .env 已被 .gitignore 排除：密钥永不进仓库、不随项目打包外流；代码里也不出现任何 key。
# 变量协议：
#   LLM_PROVIDER          激活的引擎名（deepseek / zhipu / llamacpp / ollama 或自定义）
#   LLM_<名字>_API_KEY    该引擎密钥；本地引擎（127.0.0.1）留空即可
#   LLM_<名字>_BASE_URL   可选，缺省用代码内置预设
#   LLM_<名字>_MODEL      可选（本地引擎必填），缺省用代码内置预设
# ============================================================

# —— 云端：DeepSeek ——
# LLM_PROVIDER=deepseek
# LLM_DEEPSEEK_API_KEY=sk-在此粘贴你的密钥

# —— 云端：智谱 GLM ——
# LLM_PROVIDER=zhipu
# LLM_ZHIPU_API_KEY=在此粘贴你的密钥

# —— 本地：llama.cpp server（默认 8080，model 填加载的 gguf 名）——
# LLM_PROVIDER=llamacpp
# LLM_LLAMACPP_MODEL=gemma-4-12B-it-qat-UD-Q4_K_XL
# LLM_LLAMACPP_BASE_URL=http://127.0.0.1:8080/v1/chat/completions

# —— 本地：Ollama（11434，model 填 pull 的名字）——
# LLM_PROVIDER=ollama
# LLM_OLLAMA_MODEL=qwen2.5:7b
# LLM_OLLAMA_BASE_URL=http://127.0.0.1:11434/v1/chat/completions

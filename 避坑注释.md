# 🕳️ 避坑注释（ColorBoundless 开发经验手册）

> 收集本项目从数据处理到 Agent 开发踩过的所有坑，按类别整理。
> 每条格式：【坑】→【原因】→【解法】
> 持续更新：每遇到一个新坑就补一条。

---

## 一、编码与终端

### 1. GBK 编码打不出 emoji（高频踩坑！）
- **坑**：`print("✅ 完成")` 报 `UnicodeEncodeError: 'gbk' codec can't encode character '\u2705'`
- **原因**：Windows 中文终端默认 GBK（cp936），emoji 不在 GBK 字符集
- **解法**（三选一）：
  ```python
  import sys
  sys.stdout.reconfigure(encoding="utf-8", errors="replace")  # 代码里（推荐）
  # 或运行前：$env:PYTHONIOENCODING="utf-8"
  # 或更彻底：$env:PYTHONUTF8="1"
  ```
- **原则**：**强制 UTF-8，别用 GBK**（GBK 打不出 emoji）

### 2. 终端输出乱码（GBK 显示 UTF-8 内容）
- **坑**：PowerShell 里 `Get-Content` 中文/日志乱码
- **原因**：文件是 UTF-8，终端按 GBK 解码
- **解法**：`Get-Content -Encoding utf8`；或写入文件再读取

---

## 二、路径与 sys.path

### 3. 脚本放子目录找不到包（高频踩坑！）
- **坑**：`ModuleNotFoundError: No module named 'beauty'` / `'langchain_class'`
- **原因**：`python xxx.py` 运行时 `sys.path[0]` = 脚本所在目录，不是项目根
- **解法**：脚本开头加
  ```python
  import sys
  from pathlib import Path
  sys.path.insert(0, str(Path(__file__).resolve().parents[N]))  # N=几级到根
  ```
- **注意**：层级变了（移入子目录）要重算 N。测试脚本建议统一放 `testing_scripts/` 并固定写法
- **实弹演示**：`scripts/cwd_demo.py`（从项目根和 `beauty/` 各跑一次，输出对比）：
  - `os.getcwd()` 与相对路径随 cd 漂：相对路径读 `data/shades/shades.json`，cd=根 → True，cd=beauty/ → **False**；
  - `sys.path[0]` 永远 = 脚本所在目录，与 cd 无关 → 所以 import 靠 cd 是救不了的；
  - `from lipstick import shade_library` 要同时插 **beauty/**（lipstick 所在）和**项目根**（tryon.py 的 `from cv.face_parsing import BiSeNet` 所在）——只插 beauty/ 时报 `No module named 'cv'`，铁证：**导入链穿过几个目录，就得插几个目录**；
  - `shade_library.py` 用 `Path(__file__).resolve().parents[2]` 锚定数据正本，两种 cd 下 `search_shade('FF4D6D')` 都返回 `#FF4D6D dE=0.0` —— 数据加载与 cwd 解耦的范本。

### 4. 中文路径 + cv2.imwrite 失败
- **坑**：`cv2.imwrite("E:\作业\...\x.jpg")` 静默失败（返回 False，不报错）
- **原因**：OpenCV 底层对非 ASCII 路径支持差
- **解法**：用 `cv2.imencode` + 原生 `open().write()`（见 `beauty/common.py` 的 `save_img`）

### 5. Windows Store 的 python 别名（系统级）
- **坑**：命令行 `python` 无响应/打开 Microsoft Store
- **原因**：`%LOCALAPPDATA%\Microsoft\WindowsApps\python.exe`（0 字节占位符）优先于真实 Python
- **解法**：Windows 设置 → 应用 → 高级应用设置 → 应用执行别名 → 关闭 python/python3（或管理员删除别名文件）

---

## 三、数据

### 6. 数据源偏色（本项目最大教训）
- **坑**：base_palette 处理完红色=0、橙色=0，全是绿/青/蓝
- **原因**：原始 "RGB Color Dataset" 的 R 通道全 ∈[0,4]（只是全色域一角切片）
- **解法**：换用全色域采样（`generate_full_palette.py` 步长4 → 262,144 色）
- **教训**：拿数据第一步先看分布（色相直方图），别假设"色卡=全色域"；"从图片读"与"解析文件名"结果必然一致（纯色图）

### 7. OpenCV 用 BGR 不是 RGB
- **坑**：`cv2.imread` 读出来是 BGR，直接当 RGB 用颜色就反了
- **解法**：需要 RGB 时 `cv2.cvtColor(img, cv2.COLOR_BGR2RGB)`；HEX→BGR 用 `hex2bgr`

---

## 四、依赖与环境

### 8. PyPI 默认装的是 CPU 版 torch
- **坑**：`torch.cuda.is_available()` = False（RTX 4080 没用上）
- **原因**：PyPI 上 Windows 的 torch 默认 CPU 版，CUDA 版要指定源
- **解法**：用阿里云镜像下载 wheel 本地装，或 `--index-url https://download.pytorch.org/whl/cu128`
- **验证**：`torch.cuda.is_available()` 应为 True，`nvidia-smi` 看驱动/CUDA

### 9. langchain 相关包要显式安装
- **坑**：`ModuleNotFoundError: langchain_openai` / `langchain_community` / `jieba`
- **原因**：langchain 主包不自动带这些集成/工具包
- **解法**：`pip install langchain-openai langchain-community jieba`（清华镜像加速）

### 10. LangChain 1.x API 变化
- **坑**：`from langchain.agents import AgentExecutor` 报 ImportError
- **原因**：1.x 移除了旧 API，改用 `create_agent`（LangGraph 状态图）
- **解法**：用新版 `from langchain.agents import create_agent`；查签名用 `inspect.signature()`

### 11. BiSeNet 首次运行要联网下载
- **坑**：第一次跑分割，卡在下载 resnet18 backbone（44.7MB）
- **原因**：`model.py` 里 `resnet` 用 torchvision 预训练权重
- **解法**：首次联网等它下完（缓存到 `~/.cache/torch/hub/checkpoints`），之后就快

---

## 五、模型与服务

### 12. llama.cpp 502 / 连接被拒绝
- **坑**：Agent 调本地模型报 `openai.InternalServerError: 502`，或"连接被拒绝"
- **原因**：llama.cpp 服务中途崩溃/退出；或上下文超长（历史+工具结果累积）触发
- **解法**：
  - 502 → 先 `Invoke-RestMethod /v1/models` 确认服务活着；死了就重启
  - 上下文 → 调大 `-c/--ctx-size`（如 8192），或缩短问题/检索内容
  - 模型名 → 以 `/v1/models` 返回的 id 为准（不是随便写）

### 13. 本地小模型玩复杂 Agent 容易翻车
- **坑**：14B 做多步推理/复杂决策时选错工具、忽略工具结果
- **原因**：Agent 把认知负担全压在模型上，小模型能力不足
- **解法**：见下面"架构避坑"第 14 条（意图识别 + 算法执行）

---

## 六、架构避坑（最重要）

### 14. LLM 永远别碰精确计算（D2 铁律）
- **坑**：让 LLM 处理"更红一点"这种模糊指令时，直接输出颜色值 → 翻车
- **原因**：LLM 对 RGB/Lab 数值没有精确感知
- **解法**：**LLM 只识别意图（"红色方向"），算法执行（Lab 调 a\* 或检索更红色号）**
  ```
  用户"更红一点" → Agent识别意图 → 调工具 adjust_shade(算法) → 确定色号 → 试妆
  ```
- **原则**：模糊指令→意图识别交给 LLM，精确执行交给工具/算法

### 15. 不是所有功能都要 Agent 化
- **坑**：把试妆/色差也塞进 Agent → 慢、贵、不稳
- **解法**：算法干重活、查询表干快活、RAG 干语义活、Agent 只当"总指挥"（决策+表达）

### 16. RAG vs Agent 的选择
- **坑**：简单固定问答用了 Agent（浪费），复杂任务用了 RAG（处理不了）
- **解法**：简单固定 → RAG；复杂多步 → Agent；混合：Agent 指挥 + RAG 工具

---

## 七、前端/显示

### 17. 屏幕校色偏差（美妆项目固有）
- **坑**：同一个色号在不同屏幕显示不同（手机 P3/鲜艳模式过饱和）
- **解法**：全链路 sRGB + 颜色双通道（数值+文字）+ 相对效果优先（前后/色号对比）
- **注意**：明度（L\*）跨屏幕比色相稳定，CVD 校验用明度差天然抗偏差

---

## 八、快速自查清单（出问题先看这个）

```
□ 报编码错（emoji） → 查第 1 条（UTF-8）
□ 找不到模块 → 查第 3 条（sys.path）
□ 图片保存失败 → 查第 4 条（imencode）
□ CUDA 不可用 → 查第 8 条（CPU 版 torch）
□ langchain 导入错 → 查第 10 条（1.x API）
□ 模型服务 502 → 查第 12 条（llama.cpp 状态）
□ Agent 输出颜色不对 → 查第 14 条（意图识别+算法执行）
```

---

**维护规则**：踩到新坑就补一条，保持"问题→原因→解法"三段式。答辩前通读一遍，全是真实工程经验的证据。
